import { Button } from "earthling-ui/button";

export default async function () {
  return (
    <div className="flex-1 flex items-center justify-center min-h-screen">
      <Button variant={"outline"}>Hello Earthling</Button>
    </div>
  );
}
